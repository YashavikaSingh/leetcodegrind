#include <vector>
#include <algorithm>
#include <iostream>
using namespace std;

int thirdMax(vector<int> &nums)
{

    sort(nums.begin(), nums.end());
int count = 0;

    for(int i = nums.begin(); i! = nums.end(); i++)
    {
        int ans = nums[i];
        
    }

}